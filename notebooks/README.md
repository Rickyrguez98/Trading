# Notebooks

Optional exploratory notebooks live here. Keep them out of the production
pipeline: any code that matters should live in `src/asset_selection/` and be
covered by tests.

Suggested notebooks (not committed):
- `01_universe_inspection.ipynb` — peek at the cleaned universe and counts by
  sector/exchange.
- `02_sentiment_calibration.ipynb` — compare VADER vs FinBERT on a sample.
- `03_score_distribution.ipynb` — histograms of pillar scores across the run.
